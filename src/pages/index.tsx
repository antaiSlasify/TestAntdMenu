import { useIntl } from 'react-intl';

import AppContent from '@/components/AppContent';

const Home = () => {
  return (
    <AppContent>
      <h1>HomePage</h1>
    </AppContent>
  );
};

export default Home;
